import matplotlib.pyplot as plt
#matplo... c'est module qu'on a importer pour faire des graphique qu'on racourcit en pd
import pandas as pd
import datetime
default_sd = datetime.datetime(2018,1,1,0,0,0)
#sd on décide de mettre pour startdate (idem ed = enddate)
#le = en python permet de laisser le choix à l'utilisateur de mettre la date qu'il veut sinon c'est la date qu'on à mis qui sera prise
#datetime.datetime (...) on a trouvé sur internet pour mettre la date et heure
default_ed = datetime;datetime(2021,1,1,0,0,0)

def programme_1(x,start_date = default_sd.isoformat(' ')
#????
